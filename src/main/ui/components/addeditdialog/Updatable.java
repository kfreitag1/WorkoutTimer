package ui.components.addeditdialog;

// Represents an object which can be called to update information
// Called when changes are made to a class and updates are required
public interface Updatable {
    void update();
}
